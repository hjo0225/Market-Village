# 실제 값을 채운 뒤 .env.production으로 저장(커밋 금지 — .gitignore에 이미 등록).
# docker compose --env-file .env.production -f docker-compose.prod.yml up -d --build

# MongoDB root 계정 (프로덕션 전용 — 로컬 개발 .env와 별개)
MONGO_ROOT_USER=
MONGO_ROOT_PASSWORD=

# 선택: LLM 표현 계층(대사). 비우면 오프라인 결정론 폴백으로 정상 동작.
OPENROUTER_API_KEY=
